import { AvatarId } from "@shared/types"

export const BOT_ID_PREFIX = "bot_"
export const BOT_NAME_PREFIX = "Bot "
export const BOT_AVATAR_IDS: ReadonlyArray<AvatarId> = [
  AvatarId.NERD,
  AvatarId.MONSTER,
  AvatarId.PANDA,
  AvatarId.SKULL,
  AvatarId.SQUIDWARD,
]
