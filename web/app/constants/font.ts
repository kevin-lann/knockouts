import { Bungee } from "next/font/google"
import { Fragment_Mono } from "next/font/google"

export const HEADER_FONT = Bungee({
  weight: "400",
  subsets: ["latin"],
})

export const MONO_FONT = Fragment_Mono({
  weight: "400",
  subsets: ["latin"],
})